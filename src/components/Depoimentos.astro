---
// src/components/Depoimentos.astro

// Para começar, os depoimentos podem ficar aqui. No futuro, podem virar uma coleção.
const depoimentos = [
  {
    texto: "Trabalhar com a GL Technology foi um divisor de águas. A expertise técnica e o comprometimento com os prazos superaram todas as nossas expectativas. Recomendo fortemente!",
    nome: "Ana Silva",
    cargo: "CEO da InovaTech",
    imagem: "/img/equipe/luanab.png", // Use uma imagem real
  },
  {
    texto: "A plataforma que eles desenvolveram não só é robusta e escalável, como também possui uma interface intuitiva que nossos usuários adoraram. Uma parceria de sucesso.",
    nome: "Carlos Pereira",
    cargo: "Diretor de Produtos da EduConecta",
    imagem: "/img/equipe/gustavob.jpg", // Use uma imagem real
  },
];
---

<section class="depoimentos-section">
  <div class="container">
    <div class="section-header">
      <h2 class="titulo-secao">O que nossos clientes dizem</h2>
      <p class="subtitulo-secao">A confiança que construímos é o nosso maior ativo.</p>
    </div>
    <div class="depoimentos-grid">
      {depoimentos.map(depoimento => (
        <figure class="depoimento-card">
          <blockquote>{depoimento.texto}</blockquote>
          <figcaption>
            <img src={depoimento.imagem} alt={`Foto de ${depoimento.nome}`} />
            <div class="autor-info">
              <cite class="nome-autor">{depoimento.nome}</cite>
              <span class="cargo-autor">{depoimento.cargo}</span>
            </div>
          </figcaption>
        </figure>
      ))}
    </div>
  </div>
</section>


<style>
  .depoimentos-section { padding: 80px 0; }
  .section-header { text-align: center; max-width: 600px; margin: 0 auto 60px auto; }
  .titulo-secao { font-size: 2.2rem; margin-bottom: 16px; }
  .subtitulo-secao { font-size: 1.1rem; color: var(--cor-texto-secundario); }

  .depoimentos-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(350px, 1fr)); gap: 30px; }
  .depoimento-card { margin: 0; background-color: var(--cor-fundo-secundario); padding: 32px; border-radius: 8px; display: flex; flex-direction: column; justify-content: space-between; }
  .depoimento-card blockquote { margin: 0 0 24px 0; padding: 0; border: none; font-size: 1rem; line-height: 1.7; color: var(--cor-texto-secundario); font-style: italic; }
  .depoimento-card blockquote::before { content: '“'; font-size: 3rem; color: var(--cor-destaque); float: left; margin-right: 10px; line-height: 1; }
  .depoimento-card figcaption { display: flex; align-items: center; gap: 16px; }
  .depoimento-card img { width: 50px; height: 50px; border-radius: 50%; object-fit: cover; }
  .nome-autor { font-weight: 600; color: var(--cor-texto-principal); font-style: normal; }
  .cargo-autor { font-size: 0.9rem; color: var(--cor-texto-secundario); }
</style>