---
// Arquivo: src/layouts/PaginaInterna.astro (VERSÃO CORRIGIDA E SIMPLIFICADA)

// 1. Ele apenas importa o Layout principal.
import Layout from './Layout.astro';

// 2. Ele recebe as props (como o título) para passar para o Layout principal.
interface Props {
	title?: string;
  description?: string;
}
const { title, description } = Astro.props;
---

<Layout title={title} description={description}>
  <slot />
</Layout>