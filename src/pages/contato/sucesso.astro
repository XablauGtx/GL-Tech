---
// src/pages/contato/sucesso.astro
import PaginaInterna from '../../layouts/PaginaInterna.astro';
---

<PaginaInterna title="Mensagem Enviada!">
  <div class="sucesso-container">
    <h1>Obrigado!</h1>
    <p>Sua mensagem foi enviada com sucesso. Nossa equipe entrará em contato o mais breve possível.</p>
    <a href="/" class="botao-voltar">← Voltar para a Página Inicial</a>
  </div>
</PaginaInterna>

<style>
  .sucesso-container {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    min-height: 60vh;
  }

  h1 {
    font-size: 3rem;
    color: var(--cor-destaque);
    margin-bottom: 16px;
  }

  p {
    font-size: 1.2rem;
    color: var(--cor-texto-secundario);
    max-width: 500px;
    margin-bottom: 40px;
  }

  .botao-voltar {
    display: inline-block;
    background-color: var(--cor-fundo-secundario);
    color: var(--cor-texto-principal);
    padding: 14px 28px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s;
  }
  .botao-voltar:hover {
      background-color: var(--cor-destaque);
      color: var(--cor-fundo);
  }
</style>